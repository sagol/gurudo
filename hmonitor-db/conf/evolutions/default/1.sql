# --- !Ups

create table "OBESITIES" (
"ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
"USER" BIGINT NOT NULL,
"WEIGHT" VARCHAR NOT NULL,
"BMI" FLOAT NOT NULL,
"BODY_FAT" FLOAT NOT NULL,
"WAIST_SIZE" INTEGER NOT NULL,
"WAIST_TO_HIGHT_RATIO" FLOAT NOT NULL,
"CREATE_DATE" TIMESTAMP NOT NULL,
"MODIFY_DATE" TIMESTAMP NOT NULL,
"DELETED" BOOLEAN NOT NULL
);

create table "USERS" (
"ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
"EXTERNALID" VARCHAR NOT NULL,
"EMAIL" VARCHAR NOT NULL,
"FIRST_NAME" VARCHAR NOT NULL,
"LAST_NAME" VARCHAR NOT NULL,
"MIDDLE_NAME" VARCHAR NOT NULL,
"CREATE_DATE" TIMESTAMP NOT NULL,
"MODIFY_DATE" TIMESTAMP NOT NULL,
"DELETED" BOOLEAN NOT NULL
);

alter table "OBESITIES"
add constraint "DATA_USER_FK"
foreign key("USER")
references "USERS"("ID");

# --- !Downs

alter table "OBESITIES" drop constraint "DATA_USER_FK";
drop table "OBESITIES";
drop table "USERS";